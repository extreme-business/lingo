swagger: "2.0"
info:
  title: proto/public/auth/v1/auth_service.proto
  version: version not set
tags:
  - name: AuthService
consumes:
  - application/json
produces:
  - application/json
paths:
  /v1/auth/register:
    post:
      operationId: AuthService_Register
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/v1RegisterResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/v1RegisterRequest'
      tags:
        - AuthService
definitions:
  protobufAny:
    type: object
    properties:
      '@type':
        type: string
    additionalProperties: {}
  rpcStatus:
    type: object
    properties:
      code:
        type: integer
        format: int32
      message:
        type: string
      details:
        type: array
        items:
          type: object
          $ref: '#/definitions/protobufAny'
  v1RegisterRequest:
    type: object
    properties:
      Username:
        type: string
        description: The username of a user
    required:
      - Username
  v1RegisterResponse:
    type: object
